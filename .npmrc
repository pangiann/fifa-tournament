# This is a personal project deployed by Cloudflare's build machines, which
# can only reach the public registry. Overrides any corporate registry set
# in the user's global npm config, for this folder only.
registry=https://registry.npmjs.org/
